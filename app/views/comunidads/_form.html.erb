<%= form_with(model: comunidad, local: true) do |form| %>
  <% if comunidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comunidad.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% comunidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :comunidad_title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_field :body, id: :comunidad_body, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :link %>
    <%= form.text_field :link, id: :comunidad_link, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
