<p id="notice"><%= notice %></p>

<h1 align="center">Blogs</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Texto</th>
      <th>Link</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comunidads.each do |comunidad| %>
      <tr>
        <td><%= comunidad.title %></td>
        <td><%= comunidad.body %></td>
        <td><%= comunidad.link %></td>
        <td><%= link_to 'Show', comunidad, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_comunidad_path(comunidad), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', comunidad, method: :delete, data: { confirm: 'Estas seguro?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo blog', new_comunidad_path %>
